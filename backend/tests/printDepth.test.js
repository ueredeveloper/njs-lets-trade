const printDepth = require("../utils/printDepth");

describe('printDepth', () => {
    it('deve imprimir os dados de profundidade para o par de negociação fornecido', () => {
        // Mock da função console.log para verificar se foi chamada corretamente
        //console.log = jest.fn();

        // Chama a função printDepth com um par de negociação fictício
       // printDepth('ETHUSDT')

        // Verifica se console.log foi chamado com os dados de profundidade
        //expect(console.log).toHaveBeenCalled();
    });
});
