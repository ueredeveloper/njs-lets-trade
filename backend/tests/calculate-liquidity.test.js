const calculateLiquidity = require("../utils/calculateLiquidity");

describe('Calcula a liquidez de todas moedas', () => {
    test('deve calcular a liquidez e printar os valores no console', async () => {

        // Calcula de todas moedas

        await calculateLiquidity();





    });


});

