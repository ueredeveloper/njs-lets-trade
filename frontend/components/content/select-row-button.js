const selectRowButton = () => {

    /*
    let button = document.createElement('button');
    button.textContent = 'on click row';
    button.className = 'select-row-button';
   // button.onclick = (event) => handleOnClick(event); // Changed to an arrow function
    return button;*/
    let div = document.createElement('div');

   return `<button class="select-row-button>sel row btn</button>`

  // return div;
};

export default selectRowButton;

